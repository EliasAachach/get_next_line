int		get_next_line(int fd, char **line)
{
	int 	ret;
	char	tab[BUFFER_SIZE + 1];
	char	*rest;

	while((ret = read(fd, tab, BUFFER_SIZE)) > 0)
	{
		tab[BUFFER_SIZE] = '\0';
		join(rest, tab)
		\n ? rest
		while(tab != '\n)
		{
				*line[i] = rest[i]
		}
		i++;
		rest[y] = rest[i]
		rest[y] = '\0';
		ret 1
	}
	same
}
